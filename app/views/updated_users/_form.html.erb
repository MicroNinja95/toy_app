<%= form_with(model: updated_user, local: true) do |form| %>
  <% if updated_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(updated_user.errors.count, "error") %> prohibited this updated_user from being saved:</h2>

      <ul>
      <% updated_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_name %>
    <%= form.text_area :user_name, id: :updated_user_user_name %>
  </div>

  <div class="field">
    <%= form.label :user_email %>
    <%= form.text_area :user_email, id: :updated_user_user_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
